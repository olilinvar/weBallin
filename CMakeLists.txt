cmake_minimum_required(VERSION 3.28.1)

project(My_life_is_like_a_video_game)

set(CMAKE_CXX_STANDARD 20)

add_executable(
    ${PROJECT_NAME}${PROJECT_VERSION}

    # List alle filene som skal kompileres
    # Eller link til filer i andre directories 
    # Som linker til alle filene som skal kompileres
)